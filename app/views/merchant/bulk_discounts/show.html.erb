<section id="bulk_discount">
  <div id="discount-<%= @bulk_discount.id %>">
    <h1>Bulk Discount #<%= @bulk_discount.id %></h1>
    <h3>Discount Percentage: <%= @bulk_discount.percentage %></h3>
    <h3>Quantity Threshold: <%= @bulk_discount.quantity_threshold %></h3>
    <%= button_to "Edit", edit_merchant_bulk_discount_path(@merchant, @bulk_discount.id), method: :get %>
  </div>
</section>
<h1>All Merchants</h1>

<section id="merchants">
  <ol>
    <% @merchants.each do |merchant| %>
      <li><%= link_to "#{merchant.name}", merchant_dashboard_index_path(merchant.id) %></li>
    <% end %>
  </ol>
</section>